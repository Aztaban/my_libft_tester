#include <stdio.h>
#include "../../libft.h"

size_t ft_strlcpy(char *dst, const char *src, size_t size)
{
	(void)dst; (void)src; (void)size;
	fprintf(stderr, "⚠️  ft_strlcpy is missing!\n");
	return 0;
}